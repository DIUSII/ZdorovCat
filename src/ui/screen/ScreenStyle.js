import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: '100%',
    backgroundColor: '#fff',
  },
  containerChildren: {
    height: '100%',
    paddingHorizontal: 20,
  },
});
